<% if current_user %>
  <% if @facility.favorited_by?(current_user) %>
    <%= button_to "お気に入りから削除", favorite_facility_path(@facility), method: :delete, class: "link-btn" %>
  <% else %>
    <%= button_to "お気に入りに追加", favorite_facility_path(id: @facility.id), method: :post, class: "link-btn base-color" %>
  <% end %>
<% end %>
